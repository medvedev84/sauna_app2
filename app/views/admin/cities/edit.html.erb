<div style="padding-top: 15px;">
	<div class="detail-page">
		<div class="round-box">
			<div class="round-sub-box">			
				<div class="_expanded">
					<%= form_for(@city, :url => admin_city_path(@city), :html => { :class => "comment-form" } ) do |f| %>
						<%= render 'form', :f => f %>
					<% end %>
				</div>
				<div class="_closed"></div>
				<div class="clearing"></div>			
			</div>
		</div>
	</div>
</div>