$('.row').remove();
$('.empty-row').remove();
<% if !@cities.empty? %>
	$('<%= render @cities %>').appendTo($('.result-list'));
<% else %>
	$("<%= render 'shared/empty_row' %>").appendTo($('.result-list'));
<% end %>	