var str = "";
var add_height = 30;
<% @external_payment.errors.each do |key, value| %>	
	str += '<b><%= value %></b><br/>';
	add_height += 20;
<% end %>
$('#external-payment-create .error').html(str);
$('#external-payment-create .error').show();
var old_height = parseInt($('#external-payment-create-height').val());
$('#external-payment-create').height(old_height + add_height);
