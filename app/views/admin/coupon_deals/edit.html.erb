<div style="padding-top: 15px;">
	<div class="detail-page">
		<div class="round-box">
			<div class="round-sub-box">			
				<div class="_expanded">
					<%= form_for(@coupon_deal, :url => admin_coupon_deal_path(@coupon_deal), :html => { :class => "comment-form" } ) do |f| %>
						<%= render 'form', :f => f %>
					<% end %>
				</div>
				<div class="_closed"></div>
				<div class="clearing"></div>			
			</div>
		</div>
	</div>
</div>