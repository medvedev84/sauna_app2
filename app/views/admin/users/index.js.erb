$('.row').remove();
$('.empty-row').remove();
<% if !@users.empty? %>
	$('<%= render @users %>').appendTo($('.result-list'));
	selectPaginatorLink('/admin/users', <%= @current_page_number %>);
<% else %>
	$("<%= render 'shared/empty_row' %>").appendTo($('.result-list'));
<% end %>	

