$('.row').remove();
$('.empty-row').remove();
<% if !@saunas.empty? %>
$('<%= render @saunas %>').appendTo($('.result-list'));
Frame.initSaunaSorting();
<% else %>
$("<%= render 'shared/empty_row' %>").appendTo($('.result-list'));
<% end %>